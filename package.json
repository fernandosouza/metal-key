{
  "name": "metal-metal-key",
  "version": "0.0.0",
  "description": "Adds a custom event that helps listen to key events in a cleaner way",
  "license": "BSD",
  "repository": "fernandosouza/metal-metal-key",
  "engines": {
    "node": ">=0.12.0",
    "npm": ">=3.0.0"
  },
  "main": "lib/all/metalKey.js",
  "jsnext:main": "src/MetalKey.js",
  "files": [
    "lib",
    "src",
    "test"
  ],
  "scripts": {
    "build": "gulp build",
    "compile": "babel --presets metal -d lib/ src/",
    "prepublish": "npm run compile",
    "start": "http-server . -p 4000",
    "test": "gulp test",
    "watch": "gulp build:watch"
  },
  "keywords": [
    "metal"
  ],
  "dependencies": {
    "metal": "^2.0.0",
    "metal-dom": "^2.5.3"
  },
  "devDependencies": {
    "babel-cli": "^6.4.5",
    "babel-preset-metal": "^4.0.0",
    "gulp": "^3.8.11",
    "gulp-babel": "^6.1.2",
    "gulp-metal": "^1.0.0",
    "http-server": "^0.9.0",
    "metal-component": "^2.5.10"
  }
}
